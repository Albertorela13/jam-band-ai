import { Placeholder } from "@/components/Placeholder";

const RunTest = () => (
  <Placeholder title="Run a jam session" note="The panel-running screen lands in Stage 5." />
);

export default RunTest;
