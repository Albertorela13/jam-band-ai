import { Placeholder } from "@/components/Placeholder";

const HistoryPage = () => (
  <Placeholder title="Jam history" note="Past sessions browser lands in Stage 6." />
);

export default HistoryPage;
